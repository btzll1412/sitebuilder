import React from 'react';

// Stub — full implementation in Step 5
export default function ProductsManager() {
  return <div style={{ color: 'var(--admin-text)' }}>Products Manager (Step 5)</div>;
}
